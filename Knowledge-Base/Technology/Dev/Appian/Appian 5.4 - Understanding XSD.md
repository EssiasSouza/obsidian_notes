Portanto, hoje veremos como podemos entender a configuração.

Portanto, este é o arquivo que baixei para meus dados de despesas.

E você pode ver que essa é a configuração XML e que estamos usando essas tags de

elemento e podemos ver o namespace na parte superior e o nome, que é expense data.

E é assim que podemos verificar o que está relacionado a qual tabela do banco de dados.

Você pode ver que esse é o nome e esse é o nome da minha tabela de dados, que vem do

banco de dados, e por que isso vem, por que escrevemos assim, na taxa e na taxa da coluna de taxa.

Portanto, essas são as chamadas notações.

Portanto, estamos usando essa notação para fazer a configuração da

coluna, a configuração da coluna do banco de dados.

E estamos tendo todos esses elementos, diferentes elementos diferentes.

Estamos tendo todas essas coisas e o que precisamos

fazer, por exemplo, é modificar o comprimento dessa coluna.

Em seguida, podemos alterar esse código.

Já modifiquei o valor de 500 para 1000.

É por isso que é algo assim.

Se eu quiser alterá-la novamente, só preciso modificar

a configuração dessa coluna e clicar no controle.

Então, depois de modificar isso, precisamos novamente fazer upload desse XSD e criar uma nova versão do nosso.

Portanto, esse é o processo que precisamos seguir para atualizar o direito.

Portanto, isso é tudo nesta palestra e, se houver alguma coluna nova, você

pode adicionar e, se houver algo a ser modificado, você pode atualizar.

Isso é tudo.

E agora abordaremos as palestras sobre o modelo de processo, nas quais veremos como podemos usar as

interfaces de entrada e como podemos usar os Cdts para gravar os dados no banco de dados.